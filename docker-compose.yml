services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/data:/app/app/data
    environment:
      - DATABASE_URL=sqlite:///app/data/waiswallet.db
      - GEMINI_API_KEY=${GEMINI_API_KEY}
